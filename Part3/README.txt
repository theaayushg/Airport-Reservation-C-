To build the test code:

g++ ECConsoleUITextLabel.cpp ECTextViewImp.cpp ECConsoleUIView.cpp  ECConsoleUITest.cpp -o test

To run it:

./test

It should show a window with some text for CSE 3150. At present, it doesn't do anything. Type CTRL-Q to quit.
